# APC_calculator
This program implements an Arbitrary Precision Calculator (APC) that performs addition, subtraction, multiplication, and division on integers of any size using doubly linked lists. It supports signed numbers, handles leading zeros, and avoids overflow by storing each digit as a separate node.
